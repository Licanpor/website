remify(pxsize)
  return unit(pxsize / $baseFontSize, 'rem')

center-block()
  margin-left auto
  margin-right auto

total-center()
  display flex
  align-items center
  justify-content center

v-center()
  display flex
  align-items center